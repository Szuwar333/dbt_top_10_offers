version: 2

models:
  - name: int_salary
    description: "Intermediate data for devitjobs_jobs"
    schema: intermediate
    database: mydedb
    columns:
      - name: rate
        description: "Migration batch id"
        tests:
          - not_null
          - dbt_expectations.expect_column_to_exist
